# Context Mentions Test Fixture

This is a test markdown file for the Context Mentions extension.

## File References

See `@auth` for authentication related files.

Related files: `@login` and `@src/fea`

## Examples

- Type `@` to trigger file suggestions
- Type `@auth` to filter files containing "auth"
- Type `@src/fea` to filter files in src/features path
